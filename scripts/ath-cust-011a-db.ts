import {Pool} from 'pg';import {readFileSync} from 'node:fs';import {join} from 'node:path';import {enableAppRole} from '../lib/customer/migrate.ts';
const connectionString=process.env.neon_tech_database||process.env.ASK_DATABASE_URL;if(!connectionString||connectionString==='[SENSITIVE]')throw new Error('Ask Neon credential unavailable');
const pool=new Pool({connectionString,ssl:{rejectUnauthorized:false},max:1});const client=await pool.connect();try{await client.query('BEGIN');await enableAppRole({query:(t,p)=>client.query(t,p)});const before=await client.query(`SELECT (SELECT count(*) FROM ath_hub_profiles)::text profiles,(SELECT count(*) FROM ath_claims)::text claims,(SELECT count(*) FROM ath_management_grants)::text grants`);if(process.argv.includes('--apply')){await client.query(`SET LOCAL lock_timeout='15s'`);await client.query(readFileSync(join(process.cwd(),'schema/migrations/007_ath_multi_hub_customer_foundation.sql'),'utf8'))}const constraints=await client.query(`SELECT conname,pg_get_constraintdef(oid) definition FROM pg_constraint WHERE conrelid='ath_hub_profiles'::regclass ORDER BY conname`),rls=await client.query(`SELECT count(*)::text total,count(*) FILTER(WHERE relrowsecurity)::text rls,count(*) FILTER(WHERE relforcerowsecurity)::text forced FROM pg_class WHERE relname LIKE 'ath_%' AND relkind='r'`),after=await client.query(`SELECT (SELECT count(*) FROM ath_hub_profiles)::text profiles,(SELECT count(*) FROM ath_claims)::text claims,(SELECT count(*) FROM ath_management_grants)::text grants,(SELECT count(*) FROM (SELECT hub_id,native_profile_id FROM ath_hub_profiles GROUP BY hub_id,native_profile_id HAVING count(*)>1)x)::text duplicates`);console.log(JSON.stringify({mode:process.argv.includes('--apply')?'apply':'audit',before:before.rows[0],after:after.rows[0],rls:rls.rows[0],constraints:constraints.rows},null,2));if(process.argv.includes('--apply'))await client.query('COMMIT');else await client.query('ROLLBACK')}catch(e){await client.query('ROLLBACK');throw e}finally{client.release();await pool.end()}
