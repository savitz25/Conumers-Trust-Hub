import type{CapabilityHealthState}from'./contracts/capability-health-v1.ts';
export function deriveHealth(input:{successful:boolean;contractValid:boolean;checkedAt:string;lastSuccessAt?:string|null;cadenceMinutes:number|null;knownFailure?:boolean}):{status:CapabilityHealthState;reason:string}{if(!input.contractValid)return{status:'DEGRADED',reason:'FEED_CONTRACT_MISMATCH'};if(!input.successful||input.knownFailure)return{status:'DEGRADED',reason:'POLL_FAILURE'};if(!input.lastSuccessAt||input.cadenceMinutes===null)return{status:'UNKNOWN',reason:'CADENCE_OR_SUCCESS_UNKNOWN'};const age=(Date.parse(input.checkedAt)-Date.parse(input.lastSuccessAt))/60000;return age>input.cadenceMinutes*2?{status:'DELAYED',reason:'POLL_MISSED'}:{status:'CURRENT',reason:'POLL_SUCCEEDED'}}
export function falseSilence(input:{pollSucceeded:boolean;contractValid:boolean;eventCount:number;cursorAdvanced:boolean}){if(!input.contractValid||!input.pollSucceeded)return'DEGRADED';return'CURRENT'}
