# ATH-GUIDED-001 architecture audit

The accepted classifier correctly distinguishes identifiers, identity names, complete cohorts, missing slots, and life situations. The existing Ask UI executed complete deterministic searches, but follow-up choices primarily navigated through links. It did not preserve a tab-local structured plan, execute after collecting missing slots, or normalize the three V2 result/error dialects.

The additive boundary is:

1. The accepted classifier determines the initial query.
2. Eligible Senior, Contractor, or Move requests enter a versioned session.
3. Server-authoritative transitions collect only missing structure.
4. One selected specialist V2 endpoint executes.
5. Ask normalizes public rows, refinements, provenance, limitations, and exact destinations.
6. Insurance, Lender, Investor, Concierge, homepage Intelligence, and customer systems remain unchanged.

No specialist parser, identity index, publication rule, regulatory fact, database session store, or cross-hub score was created.

ATH-GUIDED-001A corrected two state-integrity gaps without changing this boundary: history now restores the complete prior consumer state, and active refinements are visible, removable individually or together, and validated/re-executed by the server. A sessionStorage failure can disable refresh restoration without turning a successful specialist response into an error.
