# ATH-ADMIN-008 Production Activation

Status: PENDING production activation.

Canonical target is Neon `asktrusthub-platform` (`hidden-glitter-26313488`), branch `production` (`br-square-frog-aeqbig90`), database `neondb`, PostgreSQL 18.6. The non-runtime project `damp-silence-97233272` is out of scope.

This record will be completed after Migration 018 validation/application, exact candidate counts, Vercel deployment, build-specific Search RELEASE proof, regressions, and recovery rotation. It must never contain secrets, customer PII, raw specialist records, or database credentials.
