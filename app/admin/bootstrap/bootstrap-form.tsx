'use client';import{useState,type FormEvent}from'react';
export function BootstrapForm(){const[proof,setProof]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);const r=await fetch('/api/admin/bootstrap',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({proof})});const b=await r.json();setProof('');setMessage(r.ok?'Initial Super Admin created. Open /admin.':`Bootstrap denied: ${b.error??'unavailable'}`);setBusy(false)}return <form onSubmit={submit} className="card-surface mt-6 max-w-xl space-y-4 p-6"><label htmlFor="legacy-proof" className="block text-sm font-semibold">One-time legacy bootstrap proof</label><input id="legacy-proof" type="password" autoComplete="off" required value={proof} onChange={e=>setProof(e.target.value)} className="min-h-12 w-full rounded-xl border border-border px-4"/><button className="btn-primary min-h-12" disabled={busy}>{busy?'Initializing…':'Create first Super Admin'}</button><p role="status" className="text-sm">{message}</p></form>}
