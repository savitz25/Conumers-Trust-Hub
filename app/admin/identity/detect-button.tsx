'use client';import{useState}from'react';import{useRouter}from'next/navigation';export function DetectButton(){const router=useRouter(),[message,setMessage]=useState('');return <div><button className="min-h-11 rounded border px-3" onClick={async()=>{const r=await fetch('/api/admin/identity/detect',{method:'POST'});setMessage(r.ok?'Exact conflict scan complete.':'DATA_OPS authorization required.');if(r.ok)router.refresh()}}>Run exact conflict detection</button><p className="text-sm" aria-live="polite">{message}</p></div>}
