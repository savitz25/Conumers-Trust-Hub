'use client';import{useState,type FormEvent}from'react';import{useRouter}from'next/navigation';
export function ControlForm(){const router=useRouter(),[key,setKey]=useState('SEARCH_FEATURE_DISABLED'),[scopeType,setScopeType]=useState('feature'),[scopeRef,setScopeRef]=useState('example'),[reason,setReason]=useState(''),[enabled,setEnabled]=useState(false),[message,setMessage]=useState('');async function submit(e:FormEvent){e.preventDefault();const r=await fetch('/api/admin/controls',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({flagKey:key,scopeType,scopeRef,reason,enabled})}),b=await r.json();setMessage(r.ok?'Control intent saved and audited. It is not connected to product behavior.':`Denied: ${b.error??'unavailable'}`);if(r.ok)router.refresh()}return <form onSubmit={submit} className="card-surface grid gap-3 p-5 md:grid-cols-2"><label className="text-sm font-semibold">Flag key<input value={key} onChange={e=>setKey(e.target.value.toUpperCase())} className="mt-1 min-h-11 w-full rounded border border-border px-3"/></label><label className="text-sm font-semibold">Scope<select value={scopeType} onChange={e=>setScopeType(e.target.value)} className="mt-1 min-h-11 w-full rounded border border-border px-3">{['network','hub','profile_class','jurisdiction','capability','campaign','feature'].map(v=><option key={v}>{v}</option>)}</select></label><label className="text-sm font-semibold">Scope reference<input value={scopeRef} onChange={e=>setScopeRef(e.target.value)} className="mt-1 min-h-11 w-full rounded border border-border px-3"/></label><label className="text-sm font-semibold">Reason code<input required value={reason} onChange={e=>setReason(e.target.value)} className="mt-1 min-h-11 w-full rounded border border-border px-3"/></label><label className="flex min-h-11 items-center gap-2"><input type="checkbox" checked={enabled} onChange={e=>setEnabled(e.target.checked)}/> Enabled intent</label><button className="btn-primary min-h-11">Save control intent</button><p role="status" className="md:col-span-2 text-sm">{message}</p></form>}
