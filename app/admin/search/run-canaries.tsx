'use client';import{useState}from'react';
export function RunCanaries(){const[msg,setMsg]=useState('');async function run(suite:'QUICK'|'FULL'|'RELEASE'){setMsg('Running…');const r=await fetch('/api/admin/search/run',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({suite})});const b=await r.json();setMsg(r.ok?`${suite}: ${b.gate}`:`Run unavailable: ${b.error}`)}return <div className="card-surface flex flex-wrap items-center gap-2 p-4"><strong>Manual reliability proof</strong>{(['QUICK','FULL','RELEASE']as const).map(s=><button key={s} className="btn-secondary" onClick={()=>run(s)}>{s}</button>)}<span aria-live="polite" className="text-sm text-muted-foreground">{msg}</span></div>}
