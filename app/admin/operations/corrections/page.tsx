import Link from'next/link';import{redirect}from'next/navigation';import{AdminShell}from'../../admin-shell';import{withAdminSecurity}from'@/lib/control-plane/server';import{ModerationOperationsService}from'@/lib/control-plane/moderation-operations';export const dynamic='force-dynamic';export default async function Page(){let data;try{data=await withAdminSecurity(async(s,t,c,sql)=>({staff:await s.require(t,'ADMIN_VIEW'),rows:await new ModerationOperationsService(sql,s,t,c).recordIssues()}))}catch{redirect('/admin/login')}return <AdminShell staff={data.staff}><header><p className="text-xs font-semibold uppercase tracking-wider text-indigo">Trust Operations</p><h2 className="text-3xl font-semibold">Corrections</h2><p className="text-sm text-muted-foreground">Resolution follows evidence/source workflow and never silently rewrites Layer A.</p></header><ul className="space-y-3">{data.rows.map(r=><li className="card-surface p-4" key={String(r.id)}><Link className="font-semibold" href={`/admin/operations/corrections/${r.id}`}>{String(r.display_name_snapshot)} · {String(r.issue_type)}</Link><p className="text-sm">{String(r.status)} · {String(r.target_record_type)}</p></li>)}</ul>{!data.rows.length?<p>No open corrections.</p>:null}</AdminShell>}
