import{redirect}from'next/navigation';import{AdminShell}from'../../../admin-shell';import{withAdminSecurity}from'@/lib/control-plane/server';import{ModerationOperationsService}from'@/lib/control-plane/moderation-operations';import{ModerationForm}from'../../moderation-form';export const dynamic='force-dynamic';export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;let data;try{data=await withAdminSecurity(async(s,t,c,sql)=>({staff:await s.require(t,'ADMIN_VIEW'),detail:await new ModerationOperationsService(sql,s,t,c).recordIssue(id)}))}catch{redirect('/admin/login')}const i=data.detail.issue;return <AdminShell staff={data.staff}><h2 className="text-3xl font-semibold">Record issue</h2><dl className="card-surface grid gap-3 p-5 text-sm"><div><dt>Exact profile</dt><dd>{String(i.display_name_snapshot)} · {String(i.native_credential_key)}</dd></div><div><dt>Source pointer</dt><dd>{String(i.native_source_system)} · {String(i.native_profile_id)}</dd></div><div><dt>Category</dt><dd>{String(i.issue_type)} · {String(i.target_record_type)}</dd></div><div><dt>Submitted explanation</dt><dd>{String(i.explanation)}</dd></div><div><dt>Status</dt><dd>{String(i.status)} · version {String(i.version)}</dd></div></dl><ModerationForm kind="correction" id={id} version={Number(i.version)} /></AdminShell>}
