import type {ReactNode} from 'react';import type{AdminIdentity}from '@/lib/control-plane/rbac';import{AdminNav}from './admin-nav';
export function AdminShell({staff,children}:{staff:AdminIdentity;children:ReactNode}){return <main className="container-page space-y-8 py-10"><header className="space-y-4"><div><p className="text-xs font-semibold uppercase tracking-wider text-indigo">Ask Trust Hub Network Control Plane</p><h1 className="mt-1 text-3xl font-semibold text-navy">Control Center</h1><p className="mt-2 text-sm text-muted-foreground">Signed in as {staff.email} · {staff.role} · {staff.status}</p></div><AdminNav/><form action="/api/customer/auth/logout" method="post"><button className="text-sm font-semibold text-indigo underline" type="submit">Sign out</button></form></header>{children}</main>}
