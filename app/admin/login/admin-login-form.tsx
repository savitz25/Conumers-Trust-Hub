'use client';
import {useState,type FormEvent} from 'react';
export function AdminLoginForm(){const[email,setEmail]=useState(''),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);const r=await fetch('/api/customer/auth/request-link',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,next:'/admin'})});setMessage(r.ok?'Check your email for the secure sign-in link. Staff authorization is checked after sign-in.':'Sign-in is temporarily unavailable.');setBusy(false)}return <form onSubmit={submit} className="card-surface mt-6 max-w-lg space-y-4 p-6"><label className="block text-sm font-semibold" htmlFor="admin-email">Ask account email</label><input id="admin-email" type="email" required autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} className="min-h-12 w-full rounded-xl border border-border px-4"/><button disabled={busy} className="btn-primary min-h-12" type="submit">{busy?'Sending…':'Email secure sign-in link'}</button><p role="status" className="text-sm text-muted-foreground">{message}</p></form>}
