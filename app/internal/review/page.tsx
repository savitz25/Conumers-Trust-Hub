// Legacy review is retired after parity; the canonical named-staff queue remains available.
import{permanentRedirect}from'next/navigation';export default function Page(){permanentRedirect('/admin/operations/claims')}
